$(function(){ $('.datatable').DataTable({order:[[0,'desc']],pageLength:10}); $('#menuBtn').on('click',()=>$('.sidebar').toggleClass('show')); $('.scanner').trigger('focus'); });
function loadItem(serial, target){ if(!serial)return; $.get('ajax/get_item.php',{serial},function(r){ if(r.ok){ Object.keys(r.item).forEach(k=>$(`[name=${k}]`).val(r.item[k])); if(target) $(target).html(`<div class='alert alert-info'>Found: <b>${r.item.item_description}</b> | Stock: ${r.item.stock} | Status: ${r.item.status}</div>`); } else if(target) $(target).html(`<div class='alert alert-warning'>No matching item found.</div>`); },'json'); }
function openItemModal(id=''){ $('#itemForm')[0].reset(); $('#item_id').val(''); if(id){ $.get('ajax/get_item_by_id.php',{id},function(r){ if(r.ok){ Object.keys(r.item).forEach(k=>$(`[name=${k}]`).val(r.item[k])); $('#item_id').val(r.item.id); }},'json'); } new bootstrap.Modal('#itemModal').show(); }
function saveItem(){ $.post('ajax/save_item.php',$('#itemForm').serialize(),function(r){ alert(r.message); if(r.ok) location.reload(); },'json'); }
function deleteItem(id){ if(confirm('Delete this item?')) $.post('ajax/delete_item.php',{id},function(r){alert(r.message); if(r.ok) location.reload();},'json'); }
